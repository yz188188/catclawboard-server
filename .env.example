# === 本地开发 / Windows collectors 使用 (TCP 直连) ===
DATABASE_URL=mysql+pymysql://root:your-password@your-cloud-sql-ip:3306/catclawboard

# === Cloud Run 使用 (Unix socket，设置后优先于 DATABASE_URL) ===
# INSTANCE_CONNECTION_NAME=your-project:us-central1:catclawboard-db
# DB_USER=root
# DB_PASS=your-password
# DB_NAME=catclawboard

# === JWT ===
JWT_SECRET=your-secret-key-here

# === 同花顺 iFinD (仅本地 Windows 需要) ===
THS_USERNAME=your-ths-username
THS_PASSWORD=your-ths-password
